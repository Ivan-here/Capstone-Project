server.port=8082
spring.application.name=identity-service

# Mongo (local default; override in prod with env var MONGODB_URI)
spring.data.mongodb.uri=${MONGODB_URI_IDENTITY}
spring.data.mongodb.auto-index-creation=true

# JWT config (override with env var JWT_SECRET in prod)
security.jwt.secret=${JWT_SECRET:jiyyjgqevnufmhclohfveoldjuyuxcys}
security.jwt.accessTokenSeconds=3600

# Internal service-to-service secret
internal.sharedSecret=${INTERNAL_SHARED_SECRET:dev-internal-secret}